# Ferramentas

SEFAZ ES DEC: Caixa Postal expõe 1 ferramenta.

### 1. `sefaz_es_dec_caixa_postal_consultar`
**Input**: `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional), `ie` (opcional), `cnpj` (opcional), `ignora_nao_lidas` (opcional), `ignora_lidas` (opcional)

SEFAZ ES DEC: Caixa Postal, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar SEFAZ ES DEC: Caixa Postal
```
